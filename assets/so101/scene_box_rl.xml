<mujoco model="so101_lift">
  <!-- SO-101 lift task scene: Menagerie robotstudio_so101 + a graspable box + a fixed
       observation camera ("base_cam"). Reconstructs the layout used by the mjbatch-metal
       BatchPixelVecEnv example (box reset radius 0.15-0.26 m around the base). -->
  <include file="so101.xml"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="160" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 3.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" pos="0 0 0" type="plane" material="groundplane"/>
    <!-- Observation camera: in front of and above the robot, looking down at the workspace.
         MuJoCo cameras look along -Z with +Y up; this quat pitches the camera ~55 deg down
         and yaws it to face the base from +X. -->
    <camera name="base_cam" pos="0.42 -0.42 0.38" xyaxes="0.84118 0.54076 -0.00000 -0.29179 0.45389 0.84193" fovy="52"/>
    <body name="box" pos="0.2 0 0.03">
      <freejoint name="box"/>
      <geom type="box" name="box" size="0.02 0.02 0.03" condim="3" friction="1 .03 .003" rgba="0 1 0 1" contype="2"
        conaffinity="1" solref="0.01 1"/>
    </body>
  </worldbody>
</mujoco>
