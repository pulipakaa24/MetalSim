# Decisions ledger

Every option considered and decided against is kept here with its measured numbers, the reason, and
how to re-enable it, so any decision can be vetoed later. Rule (2026-09-25, from the user): options
are archived, never discarded. Code for a rejected option stays in the tree behind a flag, a branch,
or a documented setting; measurements stay in `runs/`; the row here points at both. Numbers are
measured unless marked estimated. Newer decisions go at the top.

| date | decision | options considered (with numbers) | chosen and why | how to re-enable the others |
|---|---|---|---|---|
| 2026-09-25 | Camera-RL update path | (a) PyTorch fast path + Metal kernels: update 3.22 s, 12,688 env-steps/s incl. training, identical learning curve; (b) MLX whole update fp32: 4.22 s (3.30 s best case with conv1 wgrad free); (c) MLX fp16: 3.28 s / est. 2.8 s with an fp16 kernel (different numerics); (d) conv2 weight-gradient Metal kernel: slower overall (layout copy 2.4 ms); (e) conv3 weight-gradient kernel: 1.6 % gain, under the 5 % bar | (a): fastest in fp32 with fp32-equivalent gradients | `PPOConfig.fast_update=False` for the plain path; the conv2/conv3 kernels are in `metalsim/learn/metal_conv.py`, switched off, still tested; MLX measurements in `runs/camera_update/profile_5c.log` and `docs/research/metal_cnn_update_2026-09-24.md` §7 |
| 2026-09-25 | Physics engine for parity claims | (a) MuJoCo Warp: +28.5 return at it 1000 on Isaac's flat config (Isaac +27.3), PhysX-trained policies within 5 % of their PhysX distance, protocol rows match PhysX on hold/drop, 25.7 K env-steps/s; (b) Newton XPBD fork drive 4 it/0.625 ms: +24.4, over-travel 14–23 %, joints 3 mm loose, 42.2 K; (c) Newton upstream 0.625 ms: +20.5, 61.5 K; (d) Newton 1.25 ms: +7.8, over-travel 37–48 %, 106 K | (a): fidelity first; Isaac Lab 3.0 also makes MuJoCo Warp its default solver | `G1VelocityTask(engine="newton", newton_it=4, newton_dt=0.000625, newton_kw=dict(drive="solver"))` in `.venv-newtonfork`; fork https://github.com/pulipakaa24/newton branch `metalsim`; logs `runs/g1_flat_newton*_ppo_fixed.log`; PARITY §1.5, §2.1 |
| 2026-09-25 | Newton drift remedy (archived engine) | (a) 4 it / 0.625 ms: 3.2 / 17.7 mm p50/p99, 47.2 K physics env-steps/s, 4 blown of 1024; (b) joint colouring 0.8: 3.4 / 20.1 mm, 39.5 K, 30 blown; (c) +4 joint-only passes: 5.7 / 26.3 mm, 91.5 K, drop penetration 0.40 vs 0.23 cm; (d) joint projection: 0 mm but feet pushed 67 cm through the floor; (e) Featherstone: finite only at 0.3125 ms, 12 cm contact sink, ~1 env-step/s | (a) | fork options `joint_coloring=True`, `joint_extra_iterations=k`, `SolverFeatherstone`; `runs/newton_drift_remedies_cpu.log`, `runs/newton_fork/` |
| 2026-09-25 | Newton joint drive (archived engine) | (a) fork in-solver PD drive: hands/arms/legs 1.03/1.07/1.01 of MuJoCo C step response, 3σ 0 blown, 98.6 K (30 % slower); (b) ActuatorPD (MetalSim code): 0.94/1.11/1.25, 3σ 17–19 blown, 141 K; (c) XPBD compliance drive: stiffness 72–1240 Nm/rad for ke 200 depending on iterations | (a) for fidelity; (b) kept as the code default until the fork is adopted | `NewtonSim(drive="solver"|"actuator_pd"|"xpbd")`; PR newton-physics/newton#4318 |
| 2026-09-25 | Newton limit clamp vs source fix (archived engine) | (a) clamp revolute limits inside ±(π − 0.15) on upstream Newton: 3σ 36 blown; (b) fork angle-wrap fix with original limits: 11 blown, passive energy ×0.60 | (b) where the fork is used; (a) remains for the pinned upstream install | `NewtonSim(limit_margin=0.15)` vs `None`; PR #4316 |
| 2026-09-24 | MuJoCo Warp foot contact set | (a) fork's original `plane_convex` (2 vertices on a tilted foot): Isaac's policies travel 3.01/2.94/3.18 m vs Isaac's 3.19/3.02/3.11; (b) C-exact set (4 vertices, a8e6485): 2.74/2.65/2.91 m (9–12 % short) but matches MuJoCo C's landing forces; (c) (b) + τ 10 ms impact-only stiffness + hard limits: 3.29/3.02/3.11 m (mean error 3.3 cm) | pending the contact-fidelity agent's full table (feet slide, air time, impact peaks, cost); PhysX is the reference, not MuJoCo C | switchable in the fork (see `metalsim/physics/contact_tuning.py` once merged); `runs/mjwarp_transfer_collider.log` |
| 2026-09-24 | Rough-terrain contact fix | (a) MuJoCo Warp's hfield GJK/EPA (inverted normals, launched worlds); (b) per-triangle plane-contact patch (fork f2716b4); (c) building terrain from box geoms (not tried) | (b): `test_hfield_mesh_contacts_match_mujoco_c` passes | `patches/mujoco_warp/`; PARITY §1.6 |
| 2026-09-24 | G1 physics step | (a) 5 ms (Isaac's PhysX dt): 8/8 drive blow-ups under 3σ targets in MuJoCo C; (b) 2.5 ms: 0/8, throughput halves (55.2 K → 27.7 K full loop); (c) MuJoCo Playground's 2 ms; (d) inertia-derived lower gains (mjlab): changes the task | (b) | `physics_dt` argument; `scripts/diagnostics/g1_drive_stability.py`; PARITY §1.5 |
| 2026-09-24 | Learner | (a) our Warp/PyTorch PPO after three rollout fixes (f0f2115): tracks rsl_rl within a few units, +32.8 (rough weights) / +28.4 (flat) at it 1000; (b) rsl_rl library via the VecEnv adapter: +35.5 (rough weights), +20.5 at it 399 (flat) | (a) as the product learner; (b) kept as the reference/differential | `metalsim/learn/train_g1_rslrl.py`, `metalsim/learn/rslrl_adapter.py` (extra `rslrl`) |
| 2026-09-24 | G1 reward configuration | (a) rough task's weights on flat terrain (the original port); (b) Isaac's `G1FlatEnvCfg` weights (5f208f0) | (b) default on flat; (a) kept | `G1VelocityTask(reward_cfg="rough"|"flat")` |
| 2026-09-24 | Height scan implementation | (a) Metal ray-query scan (cross-queue event inside graph capture fails); (b) Warp heightfield-interpolation kernel on the sim queue: exact vs `mj_ray` except cell diagonals | (b) | `shaders/heightscan.metal` remains for arbitrary meshes; PARITY §1.2 |
| 2026-09-24 | Benchmark reporting | (a) Isaac-style per-call mean (queue-absorbed, overstates ~2×); (b) synchronized rate | (b) headline, (a) reported alongside | `benchmark_step(..., capture=True)`; PARITY §1.4 |
| 2026-09-23 | Terrain generator | (a) re-implemented generator (up to 3.4 m height differences from Isaac's); (b) verbatim port of Isaac Lab v2.3.2 with Isaac's three RNG streams (4b23a3a): grid-exact | (b) | old generator remains in git history (`metalsim/learn/terrain.py` before 4b23a3a) |
| 2026-09-22 | Parity scene | (a) SO-101 lift scene; (b) Isaac's own benchmarked G1 velocity task on `g1_minimal.usd` | (b) per the user | `tests/test_lift_env.py`, gallery SO-101 images remain |
