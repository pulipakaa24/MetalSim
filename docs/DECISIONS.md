# Decisions ledger

Every option considered and decided against is kept here with its measured numbers, the reason, and
how to re-enable it, so any decision can be vetoed later. Rule (2026-09-25, from the user): options
are archived, never discarded. Code for a rejected option stays in the tree behind a flag, a branch,
or a documented setting; measurements stay in `runs/`; the row here points at both. Numbers are
measured unless marked estimated. Newer decisions go at the top.

| date | decision | options considered (with numbers) | chosen and why | how to re-enable the others |
|---|---|---|---|---|
| 2026-09-25 (late) | Heightfield plane-contact path for primitive geoms (fork branch `metalsim-hfield-test` `b630530`, not merged) | (a) fork `f2716b4` form: primitives use their single support point along the triangle normal → upstream's `test_hfield_maxconpair` (2 m box on a 0.2 m heightfield) 0 contacts instead of 4; (b) primitives on upstream's GJK/EPA against the prism, meshes ≤ 256 vertices keep the per-triangle plane path: fork `-k hfield` 3 passed on CPU and Metal, MetalSim `test_terrain.py` + `test_plane_convex_contacts.py` 24 passed (G1 feet are meshes, unchanged) | (b) on the branch; merge is the coordinator's call | `collision_convex.HFIELD_PLANE_CONTACTS_PRIMITIVES = True` restores (a); research note §7.4 |
| 2026-09-25 (evening) | Elliptic-cone Hessian launch form on Metal (fork `c301880`, worktree `upstream/mujoco_warp-ellip`; Go2 / G1-dense / G1-sparse / SO-101 / humanoid physics steps/s at 4096, Newton 10 / 20) | (a) upstream's off-CUDA fallback `dim_block = naconmax`: 240,281 / 60,731 / – / 441,361 / 143,607; (b) upstream's CUDA form sized from `Device.sm_count` (handoff fix 1), factor 6: 538,057 / 113,340 (Go2 factor 2: 483,040, 16: 551,979); (c) one thread per (world, entry) scanning rows: 553,510 / 126,568 / – / 837,584 / 608,507; (d) two-pass per-world cone list: 580,420 / 144,348 / – / 816,526 / 640,464; sparse assembly lanes 1 → 32: G1 77,263 → 164,231 (groups per world 4: 167,131); (e) fold the cone term into the tiled JᵀDJ build: estimated slower on the G1 (≈ 0.67 vs 0.17 ms per launch), not built; (f) incremental update extended to cone rows: estimated ≤ 10–15 %, not built; `MJW_CCD_GRID_WAVES` 2 / 8 on the G1: −7.7 % / +1.0 %; Go2 `njmax` 64: +9.9 %. Physics: CPU oracle identical for (a)–(d); Metal within the run-to-run floor | (d) default, (c)/(b)/(a) as `MJW_JTCJ_MODE=world|contact|capacity`; lanes 32; CCD grid and groups knobs kept at their previous behaviour (within noise) | env vars in `mujoco_warp/_src/solver.py` / `collision_convex.py`; `docs/research/elliptic_cones_2026-09-25.md`; logs `runs/competitors/elliptic_*.log` |
| 2026-09-25 (evening) | Elliptic cones for the G1 task, re-evaluated with the cheap kernel (supersedes option (g) of the preset row below; on top of `tau10_impact_hardlimits`, PhysX reference) | pyramidal: 20 ms force hold / drop-torso / landing 2689 / 3729 / 2707 N (Isaac 2190 / 2200 / 1996), penetration 1.64 cm, slide −0.0144 (Isaac −0.0127), air 0.0368 (0.0446), falls 142, transfer error 3 cm, 1.00×; + elliptic impratio 1: 2536 / 2607 / 2699, 1.33 cm, −0.0132, 0.0376, 171 falls, 1.41×; impratio 10: 2339 / 2379 / 2691, 1.55, −0.0131, 0.0375, 168, 1.42× (60–177 worlds at the iteration cap); impratio 100: 2328 / 2347 / 2689, 1.44, −0.0131, 0.0377, 160, 1.53× (3600–4080 worlds at the cap); impulses within 1–3 % everywhere; SO-101 grasp creep (MuJoCo C, our protocol): pyramidal 0.84 mm / 3 s, elliptic impratio 1 / 10 / 100: 4.70 / 5.24 / 1.48 mm | no default change by this agent: recommendation to the owner is elliptic + impratio 10 for the G1 (drop-torso force 1.70× → 1.08× of PhysX, slide 9 % closer, +7–10 % falls of a PhysX-trained checkpoint over 3 seeds (within one seed sd, research note §7.2), 1.42×; confirming training run `runs/il3/g1_flat_flatcfg_ellip10.log` queued) and pyramidal for the SO-101 grasp (least creep) | presets `tau10_impact_elliptic[_imp10|_imp100]_hardlimits` via `scripts/diagnostics/competitors/elliptic_presets.py`; `so101_creep.py`; research note §4 |
| 2026-09-25 | G1 throughput headline vs the task's contact preset (`docs/research/throughput_regression_2026-09-25.md`) | (a) publish the task default `contact_cfg="recommended"`: env step 54.1–54.3 K, loop 49.5–50.1 K, `solver.solve` 5.7 ms per substep, Newton cap hit in 12–28 worlds; (b) default contacts (`contact_cfg=None`): 67.1–67.2 K / 56.15 K / 4.18 ms, no cap hits (the 2026-09-25 morning headline, reproduced to 0.1 %); (c) MuJoCo Warp fork `07a51a6` vs `1791414`: ±0.4 %, not a cause; (d) machine: the 14-inch M4 Max within 2–4 % of this one, not a cause | (a): fidelity first, the preset is the PhysX-parity decision of the same day; README and PARITY §1.4 carry both numbers with their setting; the flex merge may be installed | `G1VelocityTask(contact_cfg=None)`; `MJW_TP_VARIANT='{"contact_cfg":null}'` for `g1_tp_variants.py` / `g1_step_profile.py`; fork `07a51a6` in `upstream/mujoco_warp-07a51a6` via `PYTHONPATH` |
| 2026-09-25 | MuJoCo Warp throughput options (full PPO loop at 4096 envs; adopted steps in PARITY §1.4) | rejected: `njmax` 128 / `nconmax` 16 +1.8 %; sparse constraint Jacobian +2.2 %; sparse L'DL threadgroup 16 +1.1 %; no post-reset kinematics 0 %; `WP_METAL_INFLIGHT` 16 / 256 and `WP_METAL_ICB_BATCH` 256: 0 / 0 / −11 %; blocked 16-wide Hessian Cholesky −18 %; compact register storage in the Warp Cholesky −11 %; split Hessian solve (19 + 24 dofs) 1.35 vs 1.11 ms; removing per-minibatch KL host reads < 1 % | the four adopted steps (register Cholesky to 48, sparse L'DL for M, one-world-per-thread factorization + skipped no-op launches, fused Hessian update); everything under 3 % or slower rejected | `BatchSimOptions(njmax=, nconmax=, jacobian="sparse", block_dim={"sparse_ldl_serial": 16})`; env vars; `MJW_METAL_DENSE_CHOL_MAX=32`; `runs/mjw_tp/rejected_warp_compact_register_cholesky.patch`; `scripts/diagnostics/metal_schur_cost.py`; original settings `MJW_METAL_FUSE_H_CHOLESKY=0` with variant `{"metal_register_cholesky_max":40,"m_dense_max":64}` |
| 2026-09-25 | Feet air-time / slide gap attribution | contact model (five contact settings on one policy: air 0.016–0.037) vs policy (five policies on one contact model: 0.022–0.048, seed noise ±0.0005) vs term semantics (match v2.3.2 except the slide velocity convention) | policy stage, with a secondary contact effect (impact-only stiffening halves Isaac's policy's falls here); slide convention to be fixed | `runs/contact_research/air_time/`, research note; archived: PhysX-style sensor window (no effect), discrete integrator (not available) |
| 2026-09-25 | MuJoCo Warp contact / limit preset, final (G1 flat, 2.5 ms; slide in Isaac's COM convention) | Isaac: slide −0.017, air 0.035, 20 ms force 1996/2200/2190 N, limits 0.004 rad, transfer 3.19/3.02/3.11 m; impulse within 1–4 % for every option. (a) default: force 2430/2600/2578, penetration 2.97 cm, limits 0.030, slide −0.044, air 0.022, transfer error 0.347 m, 1.00×; (b) hard limits only: 2430/2603/2571, 2.99, 0.001, −0.045, 0.022, 0.123, 1.00×; (c) tau10_impact_hardlimits: 2707/3729/2689, 1.64, 0.001, −0.032, 0.020, 0.033, chatter 112, 1.02×; (d) tau10_imp99_hardlimits: 2658/2629/2429, 1.63, 0.002, −0.032, 0.019, 0.047, 1.19×; (e) tau5_imp99_hardlimits: 2071/4245/2694 (5 ms peak above PhysX's bound), 0.85, 0.003, −0.022, 0.015, 0.143, chatter 212, 1.22×; (f) margin = gap 1 cm: stands 1 cm high; (g) elliptic cones: no slide gain, 3.84–3.95×; (h) τ 5 ms + elliptic impratio 10: diverges; (i) Isaac Lab 3.0's mapping: exceeds PhysX's bounded force; (j) old collider: slide −0.057, air 0.013, 224 falls; (k) noslip: not in MuJoCo Warp. Noise: slide ±0.0006, air ±0.0005 | (c): only option reproducing PhysX-trained policy transfer within noise, slide 28 % better than default, hard limits, penetration halved, chatter near default, torso peak at PhysX's bound, 1.02×; air time is policy stage | presets in `metalsim/physics/contact_tuning.py`; `MJW_PLANE_CONVEX=legacy`; `feet_slide_velocity="origin"`; `runs/contact_research/final_ranking_2026-09-25.md` Confirming training run (2026-09-25 evening, one seed): +27.85 / 1000 at iteration 1000 with the preset vs +28.4 / 26.3 / 26.0 with default contacts, inside seed spread (`runs/g1_flat_ppowarp_fixed_flatcfg_tau10.log`). |
| 2026-09-25 | Contact-force comparison metric | (a) sampled peak at the control step (Isaac: impulse of the last 5 ms step / 5 ms; ours: last 2.5 ms substep force): swings 203–3715 N for the same impulse depending on impact timing, 3–5× apparent gap; (b) impulse per event from the momentum change of the recorded states: Isaac vs ours 154.2 / 151.6, 143.0 / 142.7, 143.1 / 133.3 N·s (1–4 %); (c) largest 20 ms mean force: ours 18–21 % higher | (b) and (c) are the like-for-like measures; (a) kept in the tables but not used to rank | `scripts/diagnostics/contact_research/`, `runs/contact_research/`, research note |
| 2026-09-25 | MuJoCo Warp contact / limit tuning for PhysX parity (hold peak N / drop penetration / limit excursion / feet_slide / feet_air_time / transfer error / cost) | (a) default: 3715 / 2.97 cm / 0.030 / −0.048 / 0.022 / 0.35 m / 1.00×; (b) hard limits: 3710 / 2.99 / 0.001 / −0.048 / 0.022 / 0.12 / 1.00×; (c) τ 5 ms imp 0.99 + hard limits: 443 / 0.85 / 0.003 / −0.025* / 0.016* / 0.14 / 1.22×; (d) τ 10 ms imp 0.99 + hard limits: 1589 / 1.63 / 0.002 / pending / 0.047 / 1.19×; (e) τ 10 ms impact-only + hard limits: 848 / 1.64 / 0.001 / pending / 0.033 / 1.02×; (f) margin = gap 1 cm: 488 / 0 (stands 1 cm high) / 0.020 / −0.027 / 0.016 / 0.14 / 1.22×; (g) elliptic cone + hard limits: 2376–3997 / 2.5 / 0.002–0.004 / pending / 0.13 / 3.84×; (h) τ 5 ms + elliptic: 628 / 0.85 / 0.002 / −0.026 / 0.016 / 0.15 / 3.95×; (i) τ 5 ms + elliptic impratio 10: diverges in MuJoCo C; (j) Isaac Lab's ke/kd as direct solref: never falls in the drop; (k) noslip: not in MuJoCo Warp. Isaac: 712 / – / 0.004 / −0.017 / 0.035 | provisional (e): best transfer and hold peak, hard limits, 1.02×; (c) if (e)'s feet_slide is worse beyond seed noise (batch pending) | every option is a named preset in `metalsim/physics/contact_tuning.py` (`apply(m, name)` / `g1_model_tuning(name)`); logs `runs/parity/tuning*/`, `runs/contact_tuning_bench_4096_r{2,3,4}.log` |
| 2026-09-25 | MuJoCo Warp foot contact set (updates the 09-24 row) | (a) old heuristic: transfer error 0.09 m with default contacts, feet_slide −0.060, air 0.013, 234 falls; (b) C-exact set: 0.35 m with default contacts but 0.033 m with (e) above, slide −0.048, air 0.022, 30 falls, landing forces = MuJoCo C within 1e-4 | (b): better on slide, air and falls; its transfer loss was the soft default contacts | `MJW_PLANE_CONVEX=legacy`; `patches/mujoco_warp-plane-convex-c-contact-set.patch` |
| 2026-09-25 | Soft-cube contact for the flex backend (vs PhysX 5.1: bounce 0.135 m, settle 0.55 s, penetration 1.6 mm) | time-constant contact + damping 0.2: 0.132 / 0.55 / 35.7 mm; + solimp 0.99/0.999: 0.132 / 0.59 / 36.3; + 5 mm margin: 0.138 / 0.59 / 31.4 (rest pose lifted 6 mm); direct stiffness 1e6 + damping 300 + elasticity damping 0.1: 0.140 / 0.51 / 2.6; direct 2e6 + 500 + 0.15: 0.139 / 0.51 / 0.3 | direct 1e6 / 300 / 0.1 (penetration within 1 mm of PhysX, bounce and settle within noise); the stiffer alternative kept | presets in `metalsim/physics/deformable.py`; research note §12 |
| 2026-09-25 | Factorization defaults for all scenes | (a) G1-only (48, 0); (b) global (48, 0): physics at noise level, Tron1 −0.6 / −1.2 %; (c) global (48, 32): exact old M layout for trees ≤ 18 dofs, every scene within ±1 % | (c): the G1 gain with no scene slower | `BatchSimOptions(m_dense_max=None, metal_register_cholesky_max=40)` for the old behaviour; `m_dense_max=0` for the G1-only variant |
| 2026-09-25 | Rough-terrain rendering | old: no heightfield drawn, slot boxes as 2 mm cubes (tier 0 1.64 ms, tier 2 17.5 ms per 1024 × 128² frame); new: exact heightfield surface plus per-env slot boxes (2904 / 188 ms); slots only 2.75 / 59.9 ms | new by default (renders match `mj_ray` to 0.01 mm); tier-0 heightfield cost flagged, tile culling is the fix | `terrain_slots=False`, `draw_hfields=False` |
| 2026-09-25 | Flex contact cap per body–flex pair | MuJoCo's `mjMAXCONPAIR` = 50 (a 1 m 33×33 cloth needs ~290 contacts at impact and ~80 at rest, so it falls through the box; MuJoCo C 3.14 identical) vs fork option `FLEX_MAXCONPAIR` 400 (cloth rests at 0.420 m, matches a MuJoCo C build recompiled with the cap at 4000) | default 50 (parity with MuJoCo C), 400 for large cloths; upstream request to make it a model option | `collision_flex.FLEX_MAXCONPAIR`; UPSTREAM.md item 7 |
| 2026-09-25 | Deformable defaults per object type, final (vs Isaac Lab 3.0 recordings, both backends) | cloth: XPBD (top 0.414 vs PhysX 0.421 m, 312 K env-steps/s) vs flex continuum membrane (fell through the box; bug) vs physx_cloth port (5.1 particle-cloth parity only, no self-collision) vs Newton VBD (3.7 K); rope: XPBD `physical` (1.113 s vs PhysX 1.12 s at 2 cells; PhysX's sweep converges to it) vs flex (1-cell 5× too stiff) vs `physx_ref` (reproduces only a 1-cell PhysX mesh); soft volume: flex + implicit damping with PhysX's own parameters (0.152 / 0.49 vs 0.155 / 0.50) vs a PhysX-style FEM port (scoped, not needed) vs Newton VBD (0.57 K) | cloth XPBD, rope XPBD `physical`, volume flex + implicit damping, Newton backend VBD on Metal | `metalsim/physics/deformable.py` presets and `--backend`; research note §13 |
| 2026-09-25 | Rope bending stiffness: physical vs PhysX-reference | (a) Euler-Bernoulli thin-rod stiffness from the material's modulus and geometry: period 1.30 s in XPBD (small-swing estimate 1.16–1.18 s); (b) PhysX's coarse 11×2×2 hex mesh, measured 12.4× stiffer by cantilever: 1.077 s fitted vs PhysX 1.026 s. Grounding: NVIDIA's PhysX docs state a finer simulation mesh appears softer at the same Young's modulus (mesh locking); the FEM literature: first-order solid elements are too stiff in bending (shear locking) | (a) `physical` as the default (known physics); (b) `physx_ref` kept for parity comparisons; to be tested directly with a mesh-resolution sweep (2 / 4 / 8 cells across) in the Isaac Lab 3.0 recordings on both backends | rope presets in `metalsim/physics/deformable.py`; sources in `docs/research/deformables_2026-09-25.md` §11 |
| 2026-09-25 | Rope backend and parameters (vs PhysX 5.1) | XPBD thin-rod bending stiffness: period 1.30 s (correct physics for a 2 cm rod; PhysX's 11×2×2 hex mesh is 12.4× stiffer in bending, measured by cantilever); XPBD with the mesh stiffness and per-spring damping β 0.1 (20× PhysX's stated 0.005): 1.077 s / decrement 0.63 / back-swing −0.312 m, 2.57 M env-steps/s; flex implicit damping 5e-3: 1.018 s / 0.24 / −0.402 m, 9.4 K | XPBD with the fitted mesh stiffness and damping (closer on four of five measures, 270× faster); the fit is stated as such | `--backend flex`; parameters in `metalsim/physics/deformable.py`; research note §11 |
| 2026-09-25 | Flex damping | explicit (MuJoCo C's form; unstable above ~1e-4 at 0.5 ms) vs implicit backward-Euler CG in the fork (c013067; −20–25 % throughput; within 5e-5 m of explicit where both are stable) | implicit available, off by default (MuJoCo C parity untouched); on for PhysX-matching runs | fork flag; research note §11 |
| 2026-09-25 | Soft cube damping (vs PhysX 5.1) | explicit 1e-3: bounce 0.262 m / settle 1.97 s; implicit 5e-3 (PhysX's value): 0.259 / 1.52; implicit 0.2 + stiffer contact (fitted, 40× PhysX's value): 0.132 / 0.55 (PhysX 0.135 / 0.55) but penetration −3.6 cm vs PhysX −0.16 cm | fitted implicit 0.2 for now, with the penetration gap open | research note §11 |
| 2026-09-25 | Deformable defaults per object type (vs the PhysX 5.1 recording, bulk measures) | cloth: XPBD 0.425 m rest / 1.14 s settle / 3.3 cm height map, 312 K env-steps/s vs flex 3.8 cm / 2.62 s, 1.9 K; rope: flex period 1.006 s (PhysX 1.026) but log decrement 0.09 (PhysX 0.59), 11.7 K vs XPBD chain 1.323 s, 2.6 M; soft cube: flex only, bounce 0.262 m (PhysX 0.135), settle 1.97 s (0.55), 3.9 K | XPBD for cloth, flex for cables and soft volumes (fidelity first; the alternative kept for cloth and cable) | `XPBDSim` vs `DeformableSim` (`--backend`); re-decide when the Isaac Lab 3.0 recordings land |
| 2026-09-25 | Rigid/deformable coupling design | (a) single combined solve (MuJoCo Warp flex: cloth and robot in one constraint solve); (b) PhysX-style exchange on every solver iteration (needs 16× substepping of MuJoCo Warp); (c) Isaac Lab 3.0's lagged two-solver exchange (`SolverCoupledProxy`, verified from Newton 1.5.2 source; runs on Metal, 3.7 mm from CPU) | (c) is the reference arrangement for the Newton backend; (a) stays for the flex path | Newton `SolverCoupledProxy` / ADMM examples in `.venv-newton152`; `docs/research/newton_vbd_metal_2026-09-25.md` |
| 2026-09-25 | Deformable backends kept in the tree | MuJoCo Warp flex (54.1 K, correct vs MuJoCo C); XPBDSim (285 K on the PhysX-cloth test scene, 2.35 M on the small one; 5–15 cm off MuJoCo C); Newton VBD on Metal (3.7 K cloth / 0.57 K cube; Isaac Lab 3.0's own solver); `physx_cloth` PBD prototype (41.3 K; PhysX 5.6.1 algorithm, no self-collision or two-way coupling yet) | none chosen as default until the Isaac Lab 3.0 recordings exist (both backends); all four kept | `metalsim/physics/deformable.py` (`--backend`), `metalsim/physics/physx_cloth.py`, `.venv-newton152` |
| 2026-09-25 | Reference Isaac version | (a) Isaac Sim 5.1 + Isaac Lab 2.3.2 (PhysX): all references recorded so far; (b) Isaac Sim 6.1 + Isaac Lab 3.0-EA (released 2026-09-16): PhysX and Newton/MuJoCo-Warp backends, VBD deformables with coupling | (b) becomes the reference, on both backends; (a) recordings kept and labelled | `runs/parity/isaac/` (5.1) vs `runs/parity3/isaac/` (3.0); VM envs `~/env_isaaclab` (5.1) and `~/env_isaaclab3` (3.0) |
| 2026-09-25 | Deformable engine | MuJoCo Warp flex on Metal: 54.1 K env-steps/s at 4096 (cloth + cable), one step within ~2e-5 m/s of MuJoCo C; MetalSim's own XPBD cloth/cable: 2.35 M env-steps/s but 5–15 cm vertex error and +1–2 cm mean height vs MuJoCo C | MuJoCo Warp flex (fidelity first); XPBD kept with its tests and numbers | `XPBDSim`, `--backend xpbd` in `metalsim/physics/deformable.py` |
| 2026-09-25 | Flex contact model | upstream MuJoCo Warp (0.4–0.6 m/s one-step error vs MuJoCo C; cloth as a second flex fell through a box) vs MuJoCo C's rules (50-contact cap by farthest-point sampling incl. 3.14's selection quirk, 11 contacts per triangle, capsule cable segments, surface layers only) | MuJoCo C's rules | `collision_flex.ENABLE_GEOM_FLEX_FPS`, `FLEX_FPS_MODE`, `BOX_TRIANGLE_ALL_CONTACTS`, `CABLE_CAPSULE_ELEMENTS`, `FLEX_ACTIVE_LAYERS_ONLY`, `MESH_FLEX_FACE_NORMAL` |
| 2026-09-25 | Flex sorts on Metal | Warp's CPU sorts (9.1 K / 11.4 K at 256 / 1024 envs) vs GPU sorts inside the graph (17.5 K / 36.5 K) | GPU (same selections) | `FLEX_DEVICE_SORT=False` |
| 2026-09-25 | Demo cloth stretch model | edge-equality constraints vs Young's-modulus elasticity (jittery or NaN at the 2 ms step) | edge equality | `ClothCfg` XML settings |
| 2026-09-25 | Rough terrain collision surface for Isaac's vertical walls | 0.1 m heightfield: height error 99th pct 12–17 cm on wall cells, Isaac's checkpoints fall on 47/96 wall-cell starts, 29.5 K env-steps/s; heightfield 0.05 / 0.025 m: 7–14 cm, 60/96 and 64/96 falls, 27.8 K / 25.8 K; all 14,080 boxes: exact, 22/96, out of GPU memory at 4096 envs (−22 % at 1024); boxes as meshes: exact, 20/96, same limit; boxes near each robot + exact scan: exact, 14/96 (it1499 0/32), 27.8 K (−6 %) | boxes near each robot + exact scan (fidelity first: exact walls and scan, biggest transfer gain) | `G1VelocityTask(terrain_collision="hfield"|"boxes"|"meshes"|"hfield_fine"|"boxes_fine", scan_surface="grid")`, `--terrain_collision` / `--scan_surface`; `runs/terrain_walls/`; `docs/research/terrain_walls_2026-09-25.md` |
| 2026-09-25 | Rough reward set for the like-for-like run | (a) legacy "rough" (±1 sideways commands, four old formulas; never run); (b) exact G1RoughEnvCfg port `rough_isaac` | (b), fidelity rule | `reward_cfg` / `--reward_cfg` |
| 2026-09-25 | Height-scan ray order | (a) old y-fastest: 101–113 of 187 rays off Isaac's scan, up to 8 cm; (b) Isaac's x-fastest: exact | (b) default; old checkpoints permuted on load | `scan_ordering="ij"` on task, CLI, checkpoints |
| 2026-09-25 | Rough terrain seed | 0 (Isaac's env seed): origin table exact; 42 (previous task default): 0.118 m off | 0 | `--seed` |
| 2026-09-25 | Rough transfer protocol | fixed levels 3 and 6 with the curriculum off; pelvis height above local ground (z relative to the cell origin is ambiguous on stairs; both recorded); no camera to save VM time | as listed | `--level`, `LEVELS`, drop `--no_camera` in `play_policy.py` |
| 2026-09-25 | Rough per-term evaluation | mean vs stochastic actions; evaluation on levels 0–5 vs Isaac's training mix | both reported with the terrain caveat | `--stochastic`, `--seed` in `g1_reward_terms.py` |
| 2026-09-25 | Camera-RL tier-2 render default | legacy 1 spp: 30.5 ms per 1024-env step, 11,301 env-steps/s incl. PPO, return 24.8 / 45.7 at it 5 / 10; physical 4 spp exposure 1.0: 56.9 ms, 7,561, no learning (9.3 / 6.7: washed-out images); physical 4 spp exposure 0.25: 56.9 ms, 8,809, 29.5 / 45.7; 1 spp is 5.6 dB further from converged than 4 spp | physical, 4 spp, exposure 0.25 (fidelity first; the loss at 1 spp is not negligible) | `CartpoleRGBConfig(render_mode="legacy")`, `--render_mode`, `--spp`, `tier2_exposure` |
| 2026-09-25 | Gallery stage videos' renderer | old path vs preset `oidn_cal_fvg` (layered BRDFs, sun disk, 100 m clip, OIDN) | new preset for all 12 videos | `side_by_side.py --tier2_mode legacy` |
| 2026-09-25 | Rendering: BRDF for OmniPBR (robot PSNR vs RTX PT) | legacy 13.86; Storm-like curve (`omnipbr_curve`) 13.24; MDL layering base × (1 − E_ggx) (`omnipbr`) 13.73 → 28.5 with final lights; base × curve(N·V) (`omnipbr_fv`) 28.06 | `omnipbr` | `material_model=` presets in `metalsim/render/tier2.py`; `docs/research/rendering_vs_rtx_2026-09-25.md` §5 |
| 2026-09-25 | Rendering: BRDF for UsdPreviewSurface | Storm (`usd_preview_storm`); MDL albedo layering (`usd_preview`): whole vs PT 19.7; Fresnel(N·V) base (`usd_preview_fv`): 28.1 → 44.9 with far clip | `usd_preview_fv` (reproduces RTX's ground row profile to ≤ 1.5 levels) | same presets |
| 2026-09-25 | Rendering: sun units and shape | irradiance = I (UsdLux doc) misses sky + ground by 12–21 levels under every tone curve; π·I fits to 0.9 levels; delta sun vs 0.53° disk (+0.2 ms per RL batch) | π·I, disk | `usd_lights(intensity/π)`, `angle_deg=0` |
| 2026-09-25 | Rendering: tone curve and exposure | linear clamp (legacy); linear + sRGB (ground 200 vs 230); Hable (0.3 levels worse); ACES + sRGB; exposure: Kit formula 8.0e-4 (sky 162/166/174, robot 28.15) vs global fit 8.725e-4 (168/172/180, 28.50) | ACES + sRGB with the fitted exposure, stated as fitted | `tonemap="linear"`, `exposure=rtx_exposure()`, preset `tonemap_fvg` |
| 2026-09-25 | Rendering: hero-frame denoiser | none 44.16 / 28.16 (whole / robot); à-trous 42.11 / 28.39; OIDN 45.48 / 28.78 at 64 spp (vs 256 spp: 48.4 / 43.8 / 51.4 whole); MPSSVGF / MetalFX not tried (single-image temporal APIs, our frames accumulate progressively) | OIDN (+23 ms/frame) | `denoise=None` / `"atrous"` |
| 2026-09-25 | Rendering: denoiser for camera-RL batches | at 1 spp per 1024-env batch: none 29.7 dB / 11.3 ms; à-trous-5 31.1 / 24.5; à-trous-3 30.6 / 20.0; OIDN 31.0 / 116.8; 4 spp no denoiser 35.3 / 38.6 | RL default unchanged; recommendation: spend on spp, à-trous as the batched fallback | `CartpoleRGBEnv(spp=4)`; `denoise="atrous"` |
| 2026-09-25 | Rendering: far clip | none vs Isaac's 100 m: whole vs PT 28.1 → 44.9 | 100 m for the parity scene (the recorded camera) | `clip_far=0` |
| 2026-09-25 | Replicator per-pixel annotators | torch on MPS: distance 9.1 ms, normals 48.5, depth-normals 231, point cloud 209, motion 429 (per frame, 1024 envs); Warp single-pass kernels: 3.7 / 2.7 / 5.9 / 12.3 / 4.8 | Warp: identical outputs (tests run both), 2–90× cheaper | `IsaacAnnotators(impl="torch")` |
| 2026-09-25 | Replicator 2D box extents | torch `bincount` 2374 ms (CPU fallback on MPS); torch `scatter_add` 19.9; Warp 1.4 | Warp | `impl="torch"`; `_torch_extents_from_ids(counts="bincount")` |
| 2026-09-25 | Replicator loose boxes / occlusion ratio | one id re-render per object: 153 ms, exact with occlusion ratio; projected convex-hull vertices: 55 ms, exact for flat-faced meshes, slightly large for curved, no occlusion ratio; all vertices: 232 ms | re-render by default (exactness first) | `bounding_box_2d_loose(occlusion=False)`, `loose_hull=False` |
| 2026-09-25 | Replicator normals | raw half-precision renderer buffer vs renormalised | renormalised (unit length as in Isaac) | `renderer.out.normal` |
| 2026-09-25 | Replicator writer name | keep the old NPZ writer as `BasicWriter` vs Isaac's layout | Isaac's layout under `BasicWriter` (old dict input still accepted) | `NpzWriter` |
| 2026-09-25 | Camera-RL update path | (a) PyTorch fast path + Metal kernels: update 3.22 s, 12,688 env-steps/s incl. training, identical learning curve; (b) MLX whole update fp32: 4.22 s (3.30 s best case with conv1 wgrad free); (c) MLX fp16: 3.28 s / est. 2.8 s with an fp16 kernel (different numerics); (d) conv2 weight-gradient Metal kernel: slower overall (layout copy 2.4 ms); (e) conv3 weight-gradient kernel: 1.6 % gain, under the 5 % bar | (a): fastest in fp32 with fp32-equivalent gradients | `PPOConfig.fast_update=False` for the plain path; the conv2/conv3 kernels are in `metalsim/learn/metal_conv.py`, switched off, still tested; MLX measurements in `runs/camera_update/profile_5c.log` and `docs/research/metal_cnn_update_2026-09-24.md` §7 |
| 2026-09-25 | Physics engine for parity claims | (a) MuJoCo Warp: +28.5 return at it 1000 on Isaac's flat config (Isaac +27.3), PhysX-trained policies within 5 % of their PhysX distance, protocol rows match PhysX on hold/drop, 25.7 K env-steps/s; (b) Newton XPBD fork drive 4 it/0.625 ms: +24.4, over-travel 14–23 %, joints 3 mm loose, 42.2 K; (c) Newton upstream 0.625 ms: +20.5, 61.5 K; (d) Newton 1.25 ms: +7.8, over-travel 37–48 %, 106 K | (a): fidelity first; Isaac Lab 3.0 also makes MuJoCo Warp its default solver | `G1VelocityTask(engine="newton", newton_it=4, newton_dt=0.000625, newton_kw=dict(drive="solver"))` in `.venv-newtonfork`; fork https://github.com/pulipakaa24/newton branch `metalsim`; logs `runs/g1_flat_newton*_ppo_fixed.log`; PARITY §1.5, §2.1 |
| 2026-09-25 | Tier-2 firefly clamp (per-path luminance cap) | (a) off: unbiased estimator, fireflies from rare high-energy paths under a bright window remain until spp converges; (b) cap every path sample's luminance: removes them, but biased downward on bright indirect light (measured: a 0.5 furnace under a 0.2 cap reads 0.200, `test_env_randomizer_and_helpers`) | (a) as the default (fidelity first); (b) only by explicit opt-in | `Tier2Renderer(firefly_clamp=L)` / `set_firefly_clamp(L)`, `PTConsts.clamp_lum`; the docstring names the bias |
| 2026-09-25 | Environment-map sampling table | (a) uniform table (constant density: pure BSDF sampling + uniform NEE): unbiased within 5 % but [[NOISE]]× the per-pixel noise of (b) on the bright-window test; (b) luminance × sin θ table on a 512×256 grid with a 1e-6 floor (PBRT's InfiniteAreaLight), MIS power heuristic on both estimators: within 2 % of the analytic radiance; (c) finer grids or hierarchical / warp-product sampling: not built (the 512×256 table costs 10 ms on the host once per map, cached per key) | (b) | (a) is reproducible by uploading `env_sampling_table(np.ones_like(map))` as the test does; `runs/render/envmap/pytest_after.log` |
| 2026-09-25 | Dome orientation for USD / Isaac scenes | (a) the handoff's convention: pole +z, u = atan2(y, x)/2π + 0.5 (u = 0.5 faces +x); (b) USD's documented +y pole (needs an authored +90° x-rotation on z-up stages, which Kit ignores); (c) Kit/RTX's z-up treatment of the USD/OpenEXR latlong map: pole +z, image centre faces −y, u = 0.25 faces +x (NVIDIA usd-exchange-samples README, Isaac Lab authors no rotation) | (c): what Isaac renders; the azimuth reference is inferred from the pole rotation, not verified against an RTX frame (no recording has a textured dome; VM stopped) | `env_uv` / `env_dir` in `shaders/pathtrace.metal`; `set_environment(yaw=)` corrects any measured azimuth offset; research note §6.1 |
| 2026-09-25 | Environment-map table dependency | (a) OpenCV `cv2.resize(INTER_AREA)` as in the handoff (not installed in the venv); (b) numpy `add.reduceat` block average (exact area average when the grid divides the image, otherwise blocks of floor/ceil size; any table is unbiased since the density it stores is the density it samples) | (b) | `env_sampling_table` in `metalsim/render/tier2.py`; RGBE reader `metalsim/render/hdr.py` instead of an OpenCV / FreeImage dependency |
| 2026-09-25 | Newton drift remedy (archived engine) | (a) 4 it / 0.625 ms: 3.2 / 17.7 mm p50/p99, 47.2 K physics env-steps/s, 4 blown of 1024; (b) joint colouring 0.8: 3.4 / 20.1 mm, 39.5 K, 30 blown; (c) +4 joint-only passes: 5.7 / 26.3 mm, 91.5 K, drop penetration 0.40 vs 0.23 cm; (d) joint projection: 0 mm but feet pushed 67 cm through the floor; (e) Featherstone: finite only at 0.3125 ms, 12 cm contact sink, ~1 env-step/s | (a) | fork options `joint_coloring=True`, `joint_extra_iterations=k`, `SolverFeatherstone`; `runs/newton_drift_remedies_cpu.log`, `runs/newton_fork/` |
| 2026-09-25 | Newton joint drive (archived engine) | (a) fork in-solver PD drive: hands/arms/legs 1.03/1.07/1.01 of MuJoCo C step response, 3σ 0 blown, 98.6 K (30 % slower); (b) ActuatorPD (MetalSim code): 0.94/1.11/1.25, 3σ 17–19 blown, 141 K; (c) XPBD compliance drive: stiffness 72–1240 Nm/rad for ke 200 depending on iterations | (a) for fidelity; (b) kept as the code default until the fork is adopted | `NewtonSim(drive="solver"|"actuator_pd"|"xpbd")`; PR newton-physics/newton#4318 |
| 2026-09-25 | Newton limit clamp vs source fix (archived engine) | (a) clamp revolute limits inside ±(π − 0.15) on upstream Newton: 3σ 36 blown; (b) fork angle-wrap fix with original limits: 11 blown, passive energy ×0.60 | (b) where the fork is used; (a) remains for the pinned upstream install | `NewtonSim(limit_margin=0.15)` vs `None`; PR #4316 |
| 2026-09-24 | MuJoCo Warp foot contact set | (a) fork's original `plane_convex` (2 vertices on a tilted foot): Isaac's policies travel 3.01/2.94/3.18 m vs Isaac's 3.19/3.02/3.11; (b) C-exact set (4 vertices, a8e6485): 2.74/2.65/2.91 m (9–12 % short) but matches MuJoCo C's landing forces; (c) (b) + τ 10 ms impact-only stiffness + hard limits: 3.29/3.02/3.11 m (mean error 3.3 cm) | pending the contact-fidelity agent's full table (feet slide, air time, impact peaks, cost); PhysX is the reference, not MuJoCo C | switchable in the fork (see `metalsim/physics/contact_tuning.py` once merged); `runs/mjwarp_transfer_collider.log` |
| 2026-09-24 | Rough-terrain contact fix | (a) MuJoCo Warp's hfield GJK/EPA (inverted normals, launched worlds); (b) per-triangle plane-contact patch (fork f2716b4); (c) building terrain from box geoms (not tried) | (b): `test_hfield_mesh_contacts_match_mujoco_c` passes | `patches/mujoco_warp/`; PARITY §1.6 |
| 2026-09-24 | G1 physics step | (a) 5 ms (Isaac's PhysX dt): 8/8 drive blow-ups under 3σ targets in MuJoCo C; (b) 2.5 ms: 0/8, throughput halves (55.2 K → 27.7 K full loop); (c) MuJoCo Playground's 2 ms; (d) inertia-derived lower gains (mjlab): changes the task | (b) | `physics_dt` argument; `scripts/diagnostics/g1_drive_stability.py`; PARITY §1.5 |
| 2026-09-24 | Learner | (a) our Warp/PyTorch PPO after three rollout fixes (f0f2115): tracks rsl_rl within a few units, +32.8 (rough weights) / +28.4 (flat) at it 1000; (b) rsl_rl library via the VecEnv adapter: +35.5 (rough weights), +20.5 at it 399 (flat) | (a) as the product learner; (b) kept as the reference/differential | `metalsim/learn/train_g1_rslrl.py`, `metalsim/learn/rslrl_adapter.py` (extra `rslrl`) |
| 2026-09-24 | G1 reward configuration | (a) rough task's weights on flat terrain (the original port); (b) Isaac's `G1FlatEnvCfg` weights (5f208f0) | (b) default on flat; (a) kept | `G1VelocityTask(reward_cfg="rough"|"flat")` |
| 2026-09-24 | Height scan implementation | (a) Metal ray-query scan (cross-queue event inside graph capture fails); (b) Warp heightfield-interpolation kernel on the sim queue: exact vs `mj_ray` except cell diagonals | (b) | `shaders/heightscan.metal` remains for arbitrary meshes; PARITY §1.2 |
| 2026-09-24 | Benchmark reporting | (a) Isaac-style per-call mean (queue-absorbed, overstates ~2×); (b) synchronized rate | (b) headline, (a) reported alongside | `benchmark_step(..., capture=True)`; PARITY §1.4 |
| 2026-09-23 | Terrain generator | (a) re-implemented generator (up to 3.4 m height differences from Isaac's); (b) verbatim port of Isaac Lab v2.3.2 with Isaac's three RNG streams (4b23a3a): grid-exact | (b) | old generator remains in git history (`metalsim/learn/terrain.py` before 4b23a3a) |
| 2026-09-22 | Parity scene | (a) SO-101 lift scene; (b) Isaac's own benchmarked G1 velocity task on `g1_minimal.usd` | (b) per the user | `tests/test_lift_env.py`, gallery SO-101 images remain |
